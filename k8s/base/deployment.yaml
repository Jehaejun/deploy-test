apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-app
  labels:
    app: spring-app
spec:
  replicas: 1  # 기본값 (overlay에서 변경 예정)
  selector:
    matchLabels:
      app: spring-app
  template:
    metadata:
      labels:
        app: spring-app
    spec:
      containers:
        - name: spring-app
          image: spring-k8s-demo
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
